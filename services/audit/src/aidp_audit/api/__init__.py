"""AIDP Audit HTTP API.

Sub-modules:

- :mod:`aidp_audit.api.query` — ``/api/v1/audit/*`` routes
  (events list / events detail / security-events list).
- :mod:`aidp_audit.api.errors` — the AppError envelope handler.
"""

from __future__ import annotations

__version__ = "0.1.0"
