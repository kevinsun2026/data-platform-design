"""Bootstrap placeholder so mypy finds at least one module under ``services/``.

This file will be removed once the per-service scaffolds (Tasks 6-14 of the
Phase 1 plan) populate this directory with real services. It exists only to
keep ``task lint`` green on the empty monorepo so CI does not block Task 1.
"""
