"""HTTP API package for the AIDP Notify service.

Sub-modules:

- :mod:`aidp_notify.api.errors` — Unified ``AppError`` envelope
  exception handler.
- :mod:`aidp_notify.api.templates` — Template CRUD endpoints
  (``GET/POST /api/v1/notify/templates``).
- :mod:`aidp_notify.api.send` — Send endpoint + channel + log
  endpoints.
"""
