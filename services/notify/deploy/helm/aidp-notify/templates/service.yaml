apiVersion: v1
kind: Service
metadata:
  name: {{ include "aidp-notify.fullname" . }}
  labels:
    {{- include "aidp-notify.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aidp-notify.selectorLabels" . | nindent 4 }}
