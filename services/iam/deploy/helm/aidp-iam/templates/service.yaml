apiVersion: v1
kind: Service
metadata:
  name: {{ include "aidp-iam.fullname" . }}
  labels:
    {{- include "aidp-iam.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aidp-iam.selectorLabels" . | nindent 4 }}
